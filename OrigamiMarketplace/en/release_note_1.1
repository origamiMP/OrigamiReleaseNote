Feeds management :

- A new feed management was implemented in Origami
 — For this first version, only the “import CSV files” is available, new feeds will be soon available
 — Two new menus were added:
 — Feed : Add/modify a feed
 — Product : Show all the synchronised products thanks to a specific feed and you can modify and desynchronise the feed
